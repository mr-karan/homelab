---

- name: Register working directory
  set_fact:
    syncthing_project_dir: ~/services/syncthing

- name: Create project directory
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  with_items:
    - "{{syncthing_project_dir}}"

- name: Copy docker-compose file.
  template:
    src: syncthing/docker-compose.j2.yml
    dest: "{{syncthing_project_dir}}/docker-compose.yml"
  register: output

- name: Start all containers
  docker_compose:
    project_src: "{{syncthing_project_dir}}"
    state: present
    remove_orphans: true

- name: Restart all containers
  docker_compose:
    project_src: "{{syncthing_project_dir}}"
    restarted: yes
  when: output.changed

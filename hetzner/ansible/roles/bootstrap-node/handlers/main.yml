- name: restart sshd
  service:
    name: ssh
    state: restarted

- name: reboot
  reboot:

- name: Start docker on boot
  systemd:
    name: docker
    state: started
    enabled: yes

- name: Restart Docker
  systemd:
    name: docker
    state: restarted
